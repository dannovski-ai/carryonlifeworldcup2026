# Copy this to .env and fill in your values

# Port to expose (default: 3000)
PORT=3000

# Optional: football-data.org API key for automatic live/final score updates.
# Get a free key at https://www.football-data.org/client/register
# Without this, results must be entered manually in the app.
FOOTBALL_API_KEY=
